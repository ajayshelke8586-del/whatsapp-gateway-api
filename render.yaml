services:
  - type: web
    name: whatsapp-gateway-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: WEBHOOK_URL
        value: https://wacrm.panthm.com/api/webhook.php
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
